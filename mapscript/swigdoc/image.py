#: **immutable** `int` Image height in pixels
height = None

#: **immutable** `int` Image width in pixels
width = None

#: **immutable** `number` Image resolution. See See :mapfile:`RESOLUTION <map.html#index-28>`
resolution = None

#: **immutable** `number` Image resolution factor, 
#: this is :mapfile:`RESOLUTION <map.html#index-28>` / :mapfile:`DEFRESOLUTION <map.html#index-12>`.
resolutionfactor = None

#: **immutable** `string` If image is drawn by :meth:`mapObj.draw`, this is the :attr:`webObj.imagepath`
imagepath = None

#: **immutable** `string` If image is drawn by :meth:`mapObj.draw`, this is the :attr:`webObj.imageurl`
imageurl = None

#: **immutable** `string` Image :class:`outputFormatObj`
format = None
